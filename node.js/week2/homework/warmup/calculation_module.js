const add = (firstNum,secondNum) => {
        return firstNum  + secondNum
    }
    const multiply = (firstNum,secondNum) => {
        return firstNum  * secondNum

    }

module.exports = {
  'add': add ,
  'multiply': multiply
};
